# 01 · Instalação

## Aplicativo Windows (recomendado)

1. Execute o instalador WebEngineerRAG-Setup da release. Não é necessário instalar Python.
2. Abra Web Engineer RAG pelo menu Iniciar.
3. Selecione a pasta com notas Markdown e PDFs.
4. Clique em **Atualizar índice**. O aplicativo salva a configuração, baixa o modelo se necessário e mostra o progresso.
5. Ao concluir, clique em **Abrir busca**. Mantenha a janela do aplicativo aberta durante o uso.

O primeiro download requer internet. Depois, a indexação e a busca funcionam offline. Configuração, índice, modelo e logs ficam em `%LOCALAPPDATA%\WebEngineerRAG`. A instalação é por usuário, sem administrador.

Para gerar o instalador a partir dos fontes, consulte [Build Windows](07.windows-build.md).

## Instalação para desenvolvimento

Use o PowerShell na raiz do projeto, fora da pasta sincronizada do Google Drive.
O conteúdo pode continuar no Drive; o ambiente Python e o índice ficam no projeto.

## Preparar o ambiente

```powershell
powershell -ExecutionPolicy Bypass -File scripts\bootstrap.ps1
```

O script cria `.venv`, instala as dependências e baixa o modelo de embeddings.
A instalação precisa de internet. Depois do download, a busca roda localmente.
Se Python não estiver disponível, o script tenta instalá-lo via Scoop ou winget;
sem esses instaladores, instale Python 3.10+ antes de executar o comando.

## Definir o conteúdo

Se ainda não existir `.env`, copie o exemplo:

```powershell
if (-not (Test-Path .env)) { Copy-Item .env.example .env }
```

Edite `WEBRAG_SOURCE_DIR` no `.env` para apontar à pasta de notas e PDFs.
O caminho padrão é `G:\Meu Drive\ContextDirectory\Web Engineer`.
Veja os demais ajustes em [Configuração](03.configuracao.md).

## Criar o primeiro índice

```powershell
.\scripts\rag.ps1 index
.\scripts\rag.ps1 stats
```

Conclua a indexação antes de usar a interface web ou conectar um assistente.

[README](../README.md) · Anterior: [README](../README.md) · Próximo: [Uso](02.uso.md)
